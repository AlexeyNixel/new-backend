# Журнал изменений (audit log) — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Универсальный журнал изменений — при создании/изменении любой сущности (посты, отделы, теги, книги, клубы, события и т.д.) фиксируется, кто и когда это сделал, с возможностью посмотреть журнал через `GET /audit-log`.

**Architecture:** Декоратор `@Audited(entityType)` + `AuditInterceptor`, навешиваемые на конкретные `create`/`update` эндпоинты рядом с уже существующим `@UseGuards(JwtAuthGuard)`. Interceptor после успешного выполнения хендлера пишет запись через глобальный `AuditLogService` (новая таблица `audit_logs` в Prisma). Никакой Prisma-middleware/AsyncLocalStorage-магии — механизм полностью explicit, по аналогии с существующими декораторами Nest в проекте.

**Tech Stack:** NestJS 11, Prisma 6 (MySQL), Jest 30 (юнит-тесты, DB не поднимается — сервисы мокаются как в `src/auth/auth.service.spec.ts`).

**Spec:** `docs/superpowers/specs/2026-09-22-audit-log-design.md`

## Global Constraints

- Логируются только `create` и `update`. Мягкое удаление (`isDeleted=true`) — это `update`, отдельно не обрабатывается. Жёсткое удаление (`DELETE`-эндпоинты у `clubs`, `notification`, `page`) — вне охвата, декоратором не помечается.
- В записи журнала хранятся только метаданные: `entityType`, `entityId`, `action`, `userId`, `createdAt`. Без diff по полям.
- Доступ к `GET /audit-log` — под тем же `JwtAuthGuard`, что и мутирующие эндпоинты. Отдельной ролевой модели не вводим (её нет в проекте).
- Если модуль-цель ещё не защищён `JwtAuthGuard` на `create`/`update` (сейчас так у `achievements` и `clubs`) — добавляем `@UseGuards(JwtAuthGuard)` заодно: без аутентификации `AuditInterceptor` физически не может узнать, кто автор изменения, а это и есть цель задачи.
- Ошибка записи в журнал никогда не должна ронять основной запрос пользователя (try/catch + `Logger.warn` внутри `AuditInterceptor`).
- Стиль кода — Prettier (одинарные кавычки, `trailingComma: all`), как и остальной проект.
- Jest-тесты не резолвят bare-путь `generated/prisma` (`baseUrl` из tsconfig не подхватывается jest-ом, `rootDir: "src"`, `moduleNameMapper` не настроен) — везде, где спек транзитивно тянет `PrismaService` или `Prisma`/`AuditAction` из `generated/prisma`, нужен `jest.mock(...)`, по образцу `src/auth/auth.service.spec.ts:8`.

---

### Task 1: Модель `AuditLog` в Prisma-схеме

**Files:**
- Modify: `prisma/schema.prisma`
- Migration: новая папка в `prisma/migrations/` (создаётся командой, не руками)

**Interfaces:**
- Produces: Prisma-модель `AuditLog` (поля `id`, `entityType`, `entityId`, `action: AuditAction`, `userId`, `createdAt`, relation `user`), enum `AuditAction` (`CREATE`, `UPDATE`), обратная связь `User.auditLogs`. После `npx prisma generate` доступны как `PrismaService.auditLog.*` и типы `AuditAction`, `Prisma.AuditLogCreateInput` и т.д. из `generated/prisma`.

- [ ] **Step 1: Добавить enum и модель в `prisma/schema.prisma`**

Добавить в конец файла (после последнего enum, например после `NotificationType`):

```prisma
enum AuditAction {
  CREATE
  UPDATE
}

model AuditLog {
  id         String      @id @default(uuid())
  entityType String      @db.VarChar(100)
  entityId   String
  action     AuditAction
  userId     String?
  user       User?       @relation(fields: [userId], references: [id], onDelete: SetNull)
  createdAt  DateTime    @default(now())

  @@index([entityType, entityId])
  @@index([userId])
  @@map("audit_logs")
}
```

- [ ] **Step 2: Добавить обратную связь в `model User`**

В `prisma/schema.prisma` текущая модель `User` (строки 12-20):

```prisma
model User {
  id        String   @id @default(uuid())
  username  String   @unique
  password  String
  name      String?
  createdAt DateTime @default(now())

  @@map("users")
}
```

Заменить на:

```prisma
model User {
  id        String     @id @default(uuid())
  username  String     @unique
  password  String
  name      String?
  createdAt DateTime   @default(now())
  auditLogs AuditLog[]

  @@map("users")
}
```

- [ ] **Step 3: Провалидировать схему**

Run: `npx prisma validate`
Expected: `The schema at prisma/schema.prisma is valid 🚀`

- [ ] **Step 4: Сгенерировать и применить миграцию**

Run: `npx prisma migrate dev --name audit_log`
Expected: новая папка `prisma/migrations/<timestamp>_audit_log/migration.sql` с `CREATE TABLE audit_logs (...)`, команда завершается `Your database is now in sync with your schema.` Заодно перегенерирует Prisma Client (`generated/prisma`).

- [ ] **Step 5: Проверить, что клиент сгенерирован с новой моделью**

Run: `grep -n "AuditAction" generated/prisma/index.d.ts | head -3`
Expected: непустой вывод (тип `AuditAction` присутствует в сгенерированных типах).

- [ ] **Step 6: Commit**

```bash
git add prisma/schema.prisma prisma/migrations
git commit -m "feat: добавить модель AuditLog для журнала изменений"
```

---

### Task 2: Декоратор `@Audited(entityType)`

**Files:**
- Create: `src/common/decorators/audited.decorator.ts`
- Test: `src/common/decorators/audited.decorator.spec.ts`

**Interfaces:**
- Produces: `AUDITED_ENTITY_KEY` (строковая metadata-константа), `Audited(entityType: string)` — декоратор метода контроллера. Используется в Task 4 (`AuditInterceptor` читает эту metadata) и во всех Task 7-21 (применяется на контроллерах).

- [ ] **Step 1: Написать падающий тест**

Создать `src/common/decorators/audited.decorator.spec.ts`:

```ts
import { Reflector } from '@nestjs/core';
import { Audited, AUDITED_ENTITY_KEY } from './audited.decorator';

class TestController {
  @Audited('Post')
  create() {
    return null;
  }

  update() {
    return null;
  }
}

describe('@Audited', () => {
  it('проставляет metadata с типом сущности на помеченном методе', () => {
    const reflector = new Reflector();

    const metadata = reflector.get<string>(
      AUDITED_ENTITY_KEY,
      TestController.prototype.create,
    );

    expect(metadata).toBe('Post');
  });

  it('не проставляет metadata на непомеченном методе', () => {
    const reflector = new Reflector();

    const metadata = reflector.get<string>(
      AUDITED_ENTITY_KEY,
      TestController.prototype.update,
    );

    expect(metadata).toBeUndefined();
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx jest audited.decorator.spec.ts`
Expected: FAIL с `Cannot find module './audited.decorator'`

- [ ] **Step 3: Реализовать декоратор**

Создать `src/common/decorators/audited.decorator.ts`:

```ts
import { SetMetadata } from '@nestjs/common';

export const AUDITED_ENTITY_KEY = 'audited-entity';

export const Audited = (entityType: string) =>
  SetMetadata(AUDITED_ENTITY_KEY, entityType);
```

- [ ] **Step 4: Прогнать тест, убедиться, что проходит**

Run: `npx jest audited.decorator.spec.ts`
Expected: PASS (2 теста)

- [ ] **Step 5: Commit**

```bash
git add src/common/decorators/audited.decorator.ts src/common/decorators/audited.decorator.spec.ts
git commit -m "feat: добавить декоратор @Audited"
```

---

### Task 3: `AuditLogService`

**Files:**
- Create: `src/audit-log/audit-log.service.ts`
- Test: `src/audit-log/audit-log.service.spec.ts`

**Interfaces:**
- Consumes: `PrismaService` (из `../prisma.service`, инжектится как обычно).
- Produces: `CreateAuditLogEntry` (`{ entityType: string; entityId: string; action: AuditAction; userId?: string | null }`), `AuditLogFilters` (`{ entityType?: string; entityId?: string }`), `AuditLogService.log(entry: CreateAuditLogEntry): Promise<AuditLog>`, `AuditLogService.findAll(filters: AuditLogFilters, page: number, limit: number): Promise<{ items: AuditLogWithUser[]; total: number }>`. Используется в Task 4 (`AuditInterceptor`) и Task 6 (`AuditLogController`).

- [ ] **Step 1: Написать падающий тест**

Создать `src/audit-log/audit-log.service.spec.ts`:

```ts
// AuditLogService зависит от PrismaService, который тянет generated/prisma —
// bare-путь не резолвится в jest (см. src/auth/auth.service.spec.ts). Подменяем заглушкой.
jest.mock('../prisma.service', () => ({ PrismaService: class {} }));

import { AuditLogService } from './audit-log.service';

describe('AuditLogService', () => {
  const makePrismaMock = () => ({
    auditLog: {
      create: jest.fn(),
      findMany: jest.fn(),
      count: jest.fn(),
    },
  });

  describe('log', () => {
    it('пишет запись через prisma.auditLog.create', async () => {
      const prismaMock = makePrismaMock();
      prismaMock.auditLog.create.mockResolvedValue({ id: 'log1' });
      const service = new AuditLogService(prismaMock as never);

      await service.log({
        entityType: 'Post',
        entityId: 'post1',
        action: 'UPDATE' as never,
        userId: 'user1',
      });

      expect(prismaMock.auditLog.create).toHaveBeenCalledWith({
        data: {
          entityType: 'Post',
          entityId: 'post1',
          action: 'UPDATE',
          userId: 'user1',
        },
      });
    });

    it('пишет userId как null, если он не передан', async () => {
      const prismaMock = makePrismaMock();
      prismaMock.auditLog.create.mockResolvedValue({ id: 'log1' });
      const service = new AuditLogService(prismaMock as never);

      await service.log({
        entityType: 'Post',
        entityId: 'post1',
        action: 'CREATE' as never,
      });

      expect(prismaMock.auditLog.create).toHaveBeenCalledWith({
        data: {
          entityType: 'Post',
          entityId: 'post1',
          action: 'CREATE',
          userId: null,
        },
      });
    });
  });

  describe('findAll', () => {
    it('фильтрует по entityType/entityId, считает total и применяет пагинацию', async () => {
      const prismaMock = makePrismaMock();
      prismaMock.auditLog.findMany.mockResolvedValue([{ id: 'log1' }]);
      prismaMock.auditLog.count.mockResolvedValue(1);
      const service = new AuditLogService(prismaMock as never);

      const result = await service.findAll(
        { entityType: 'Post', entityId: 'post1' },
        2,
        10,
      );

      expect(prismaMock.auditLog.findMany).toHaveBeenCalledWith({
        where: { entityType: 'Post', entityId: 'post1' },
        include: { user: { select: { id: true, username: true, name: true } } },
        orderBy: { createdAt: 'desc' },
        skip: 10,
        take: 10,
      });
      expect(prismaMock.auditLog.count).toHaveBeenCalledWith({
        where: { entityType: 'Post', entityId: 'post1' },
      });
      expect(result).toEqual({ items: [{ id: 'log1' }], total: 1 });
    });

    it('без фильтров передаёт пустой where', async () => {
      const prismaMock = makePrismaMock();
      prismaMock.auditLog.findMany.mockResolvedValue([]);
      prismaMock.auditLog.count.mockResolvedValue(0);
      const service = new AuditLogService(prismaMock as never);

      await service.findAll({}, 1, 10);

      expect(prismaMock.auditLog.findMany).toHaveBeenCalledWith(
        expect.objectContaining({ where: {}, skip: 0, take: 10 }),
      );
    });
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx jest audit-log.service.spec.ts`
Expected: FAIL с `Cannot find module './audit-log.service'`

- [ ] **Step 3: Реализовать сервис**

Создать `src/audit-log/audit-log.service.ts`:

```ts
import { Injectable } from '@nestjs/common';
import { PrismaService } from '../prisma.service';
import type { AuditAction } from 'generated/prisma';

export interface CreateAuditLogEntry {
  entityType: string;
  entityId: string;
  action: AuditAction;
  userId?: string | null;
}

export interface AuditLogFilters {
  entityType?: string;
  entityId?: string;
}

@Injectable()
export class AuditLogService {
  constructor(private readonly prismaService: PrismaService) {}

  log(entry: CreateAuditLogEntry) {
    return this.prismaService.auditLog.create({
      data: {
        entityType: entry.entityType,
        entityId: entry.entityId,
        action: entry.action,
        userId: entry.userId ?? null,
      },
    });
  }

  async findAll(filters: AuditLogFilters, page: number, limit: number) {
    const where = {
      ...(filters.entityType ? { entityType: filters.entityType } : {}),
      ...(filters.entityId ? { entityId: filters.entityId } : {}),
    };

    const [items, total] = await Promise.all([
      this.prismaService.auditLog.findMany({
        where,
        include: {
          user: { select: { id: true, username: true, name: true } },
        },
        orderBy: { createdAt: 'desc' },
        skip: (page - 1) * limit,
        take: limit,
      }),
      this.prismaService.auditLog.count({ where }),
    ]);

    return { items, total };
  }
}
```

Обратите внимание: `AuditAction` импортирован через `import type` — это только тип, к рантайму не относится, поэтому `generated/prisma` не резолвится при запуске юнит-теста (мок `../prisma.service` полностью закрывает реальный `PrismaService` и его собственный импорт `generated/prisma`).

- [ ] **Step 4: Прогнать тест, убедиться, что проходит**

Run: `npx jest audit-log.service.spec.ts`
Expected: PASS (4 теста)

- [ ] **Step 5: Commit**

```bash
git add src/audit-log/audit-log.service.ts src/audit-log/audit-log.service.spec.ts
git commit -m "feat: добавить AuditLogService"
```

---

### Task 4: `AuditInterceptor`

**Files:**
- Create: `src/common/interceptors/audit.interceptor.ts`
- Test: `src/common/interceptors/audit.interceptor.spec.ts`

**Interfaces:**
- Consumes: `AUDITED_ENTITY_KEY` из `../decorators/audited.decorator` (Task 2), `AuditLogService.log(entry)` из `../../audit-log/audit-log.service` (Task 3), `Reflector` из `@nestjs/core`.
- Produces: `AuditInterceptor` (`NestInterceptor`) — регистрируется в Task 5 (`AuditLogModule`) как provider, используется через `@UseInterceptors(AuditInterceptor)` в Task 7-21.

- [ ] **Step 1: Написать падающий тест**

Создать `src/common/interceptors/audit.interceptor.spec.ts`:

```ts
// AuditInterceptor использует AuditAction как значение (не только тип) —
// нужен реальный импорт generated/prisma, который не резолвится в jest.
// Подменяем оба модуля заглушками.
jest.mock('generated/prisma', () => ({
  AuditAction: { CREATE: 'CREATE', UPDATE: 'UPDATE' },
}));
jest.mock('../../audit-log/audit-log.service', () => ({
  AuditLogService: class {},
}));

import { of, throwError } from 'rxjs';
import { CallHandler, ExecutionContext } from '@nestjs/common';
import { Reflector } from '@nestjs/core';
import { AuditInterceptor } from './audit.interceptor';
import { AUDITED_ENTITY_KEY } from '../decorators/audited.decorator';

describe('AuditInterceptor', () => {
  const makeContext = (
    method: string,
    user: unknown,
    handler: () => void = function create() {},
  ): ExecutionContext =>
    ({
      getHandler: () => handler,
      switchToHttp: () => ({
        getRequest: () => ({ method, user }),
      }),
    }) as unknown as ExecutionContext;

  const makeHandler = (response: unknown): CallHandler => ({
    handle: () => of(response),
  });

  it('пропускает поток без записи в лог, если нет @Audited metadata', (done) => {
    const reflector = { get: jest.fn().mockReturnValue(undefined) };
    const auditLogService = { log: jest.fn() };
    const interceptor = new AuditInterceptor(
      reflector as unknown as Reflector,
      auditLogService as never,
    );
    const context = makeContext('POST', { id: 'user1' });

    interceptor
      .intercept(context, makeHandler({ id: 'post1' }))
      .subscribe(() => {
        expect(auditLogService.log).not.toHaveBeenCalled();
        done();
      });
  });

  it('пишет CREATE для POST-запроса с @Audited', (done) => {
    const reflector = { get: jest.fn().mockReturnValue('Post') };
    const auditLogService = { log: jest.fn().mockResolvedValue(undefined) };
    const interceptor = new AuditInterceptor(
      reflector as unknown as Reflector,
      auditLogService as never,
    );
    const context = makeContext('POST', { id: 'user1' });

    interceptor
      .intercept(context, makeHandler({ id: 'post1' }))
      .subscribe(() => {
        setImmediate(() => {
          expect(auditLogService.log).toHaveBeenCalledWith({
            entityType: 'Post',
            entityId: 'post1',
            action: 'CREATE',
            userId: 'user1',
          });
          done();
        });
      });
  });

  it('пишет UPDATE для PATCH-запроса с @Audited', (done) => {
    const reflector = { get: jest.fn().mockReturnValue('Post') };
    const auditLogService = { log: jest.fn().mockResolvedValue(undefined) };
    const interceptor = new AuditInterceptor(
      reflector as unknown as Reflector,
      auditLogService as never,
    );
    const context = makeContext('PATCH', { id: 'user1' });

    interceptor
      .intercept(context, makeHandler({ id: 'post1' }))
      .subscribe(() => {
        setImmediate(() => {
          expect(auditLogService.log).toHaveBeenCalledWith({
            entityType: 'Post',
            entityId: 'post1',
            action: 'UPDATE',
            userId: 'user1',
          });
          done();
        });
      });
  });

  it('userId = null, если request.user не установлен', (done) => {
    const reflector = { get: jest.fn().mockReturnValue('Post') };
    const auditLogService = { log: jest.fn().mockResolvedValue(undefined) };
    const interceptor = new AuditInterceptor(
      reflector as unknown as Reflector,
      auditLogService as never,
    );
    const context = makeContext('POST', undefined);

    interceptor
      .intercept(context, makeHandler({ id: 'post1' }))
      .subscribe(() => {
        setImmediate(() => {
          expect(auditLogService.log).toHaveBeenCalledWith(
            expect.objectContaining({ userId: null }),
          );
          done();
        });
      });
  });

  it('не пишет лог, если в ответе нет id', (done) => {
    const reflector = { get: jest.fn().mockReturnValue('Post') };
    const auditLogService = { log: jest.fn() };
    const interceptor = new AuditInterceptor(
      reflector as unknown as Reflector,
      auditLogService as never,
    );
    const context = makeContext('POST', { id: 'user1' });

    interceptor
      .intercept(context, makeHandler({ message: 'slug занят' }))
      .subscribe(() => {
        expect(auditLogService.log).not.toHaveBeenCalled();
        done();
      });
  });

  it('не ломает поток, если запись в лог падает с ошибкой', (done) => {
    const reflector = { get: jest.fn().mockReturnValue('Post') };
    const auditLogService = {
      log: jest.fn().mockRejectedValue(new Error('db down')),
    };
    const interceptor = new AuditInterceptor(
      reflector as unknown as Reflector,
      auditLogService as never,
    );
    const context = makeContext('POST', { id: 'user1' });

    interceptor
      .intercept(context, makeHandler({ id: 'post1' }))
      .subscribe((response) => {
        expect(response).toEqual({ id: 'post1' });
        done();
      });
  });

  it('не вызывает лог, если основной хендлер бросил ошибку', (done) => {
    const reflector = { get: jest.fn().mockReturnValue('Post') };
    const auditLogService = { log: jest.fn() };
    const interceptor = new AuditInterceptor(
      reflector as unknown as Reflector,
      auditLogService as never,
    );
    const context = makeContext('POST', { id: 'user1' });
    const failingHandler: CallHandler = {
      handle: () => throwError(() => new Error('validation failed')),
    };

    interceptor.intercept(context, failingHandler).subscribe({
      error: () => {
        expect(auditLogService.log).not.toHaveBeenCalled();
        done();
      },
    });
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx jest audit.interceptor.spec.ts`
Expected: FAIL с `Cannot find module './audit.interceptor'`

- [ ] **Step 3: Реализовать interceptor**

Создать `src/common/interceptors/audit.interceptor.ts`:

```ts
import {
  CallHandler,
  ExecutionContext,
  Injectable,
  Logger,
  NestInterceptor,
} from '@nestjs/common';
import { Reflector } from '@nestjs/core';
import { Observable, tap } from 'rxjs';
import type { Request } from 'express';
import { AuditAction } from 'generated/prisma';
import { AUDITED_ENTITY_KEY } from '../decorators/audited.decorator';
import { AuditLogService } from '../../audit-log/audit-log.service';

@Injectable()
export class AuditInterceptor implements NestInterceptor {
  private readonly logger = new Logger(AuditInterceptor.name);

  constructor(
    private readonly reflector: Reflector,
    private readonly auditLogService: AuditLogService,
  ) {}

  intercept(context: ExecutionContext, next: CallHandler): Observable<unknown> {
    const entityType = this.reflector.get<string | undefined>(
      AUDITED_ENTITY_KEY,
      context.getHandler(),
    );

    if (!entityType) {
      return next.handle();
    }

    const request = context.switchToHttp().getRequest<Request>();
    const action =
      request.method === 'POST' ? AuditAction.CREATE : AuditAction.UPDATE;
    const userId = (request.user as { id?: string } | undefined)?.id ?? null;

    return next.handle().pipe(
      tap((response: unknown) => {
        const entityId = (response as { id?: string } | null)?.id;
        if (!entityId) {
          return;
        }

        this.auditLogService
          .log({ entityType, entityId, action, userId })
          .catch((error: Error) => {
            this.logger.warn(
              `Не удалось записать audit-лог для ${entityType}#${entityId}: ${error.message}`,
            );
          });
      }),
    );
  }
}
```

- [ ] **Step 4: Прогнать тест, убедиться, что проходит**

Run: `npx jest audit.interceptor.spec.ts`
Expected: PASS (7 тестов)

- [ ] **Step 5: Commit**

```bash
git add src/common/interceptors/audit.interceptor.ts src/common/interceptors/audit.interceptor.spec.ts
git commit -m "feat: добавить AuditInterceptor"
```

---

### Task 5: `AuditLogModule` (глобальный модуль)

**Files:**
- Create: `src/audit-log/audit-log.module.ts`
- Modify: `src/app.module.ts`

**Interfaces:**
- Consumes: `AuditLogService` (Task 3), `AuditInterceptor` (Task 4), `AuditLogController` (создаётся в Task 6 — на этом шаге контроллер ещё не существует, поэтому `controllers` пока не заполняем, добавим в Task 6).
- Produces: `AuditLogModule`, зарегистрированный как `@Global()` в `AppModule` — `AuditLogService` и `AuditInterceptor` становятся доступны для инъекции в любом модуле без явного импорта.

- [ ] **Step 1: Создать модуль**

Создать `src/audit-log/audit-log.module.ts`:

```ts
import { Global, Module } from '@nestjs/common';
import { AuditLogService } from './audit-log.service';
import { AuditInterceptor } from '../common/interceptors/audit.interceptor';

@Global()
@Module({
  providers: [AuditLogService, AuditInterceptor],
  exports: [AuditLogService, AuditInterceptor],
})
export class AuditLogModule {}
```

(`PrismaService` не указываем как provider — `PrismaModule` уже `@Global()` и экспортирует его, см. `src/prisma.module.ts`.)

- [ ] **Step 2: Зарегистрировать модуль в `AppModule`**

В `src/app.module.ts` добавить импорт после `import { MapPointModule } from './map-point/map-point.module';`:

```ts
import { AuditLogModule } from './audit-log/audit-log.module';
```

И добавить `AuditLogModule` в массив `imports` (после `MapPointModule`):

```ts
    MapPointModule,
    AuditLogModule,
```

- [ ] **Step 3: Проверить, что приложение собирается**

Run: `npx tsc --noEmit -p tsconfig.build.json`
Expected: без ошибок (модуль синтаксически корректен; на этом шаге `AuditInterceptor`/`AuditLogService` ещё нигде не используются через `@UseInterceptors`, но должны компилироваться).

- [ ] **Step 4: Commit**

```bash
git add src/audit-log/audit-log.module.ts src/app.module.ts
git commit -m "feat: зарегистрировать глобальный AuditLogModule"
```

---

### Task 6: `AuditLogController` (`GET /audit-log`)

**Files:**
- Create: `src/audit-log/audit-log.controller.ts`
- Modify: `src/audit-log/audit-log.module.ts`

**Interfaces:**
- Consumes: `AuditLogService.findAll(filters, page, limit)` (Task 3), `PaginationQueryDto` (существующий `src/common/dto/pagination-query.dto.ts`), `ResponseService.paginated(...)` (существующий `src/common/services/response.service.ts`), `JwtAuthGuard` (существующий `src/auth/guards/jwt-auth.guard.ts`).
- Produces: эндпоинт `GET /audit-log?entityType=&entityId=&page=&limit=`.

- [ ] **Step 1: Реализовать контроллер**

Создать `src/audit-log/audit-log.controller.ts`:

```ts
import { Controller, Get, Query, UseGuards } from '@nestjs/common';
import { AuditLogService } from './audit-log.service';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { ResponseService } from '../common/services/response.service';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';

@UseGuards(JwtAuthGuard)
@Controller('audit-log')
export class AuditLogController {
  constructor(
    private readonly auditLogService: AuditLogService,
    private readonly responseService: ResponseService,
  ) {}

  @Get()
  async findAll(
    @Query() paginationQuery: PaginationQueryDto,
    @Query('entityType') entityType: string,
    @Query('entityId') entityId: string,
  ) {
    const page = Number(paginationQuery.page) || 1;
    const limit = Number(paginationQuery.limit) || 10;

    const { items, total } = await this.auditLogService.findAll(
      { entityType, entityId },
      page,
      limit,
    );

    return this.responseService.paginated(items, total, page, limit);
  }
}
```

- [ ] **Step 2: Подключить контроллер и `ResponseService` в модуль**

В `src/audit-log/audit-log.module.ts` заменить содержимое на:

```ts
import { Global, Module } from '@nestjs/common';
import { AuditLogService } from './audit-log.service';
import { AuditLogController } from './audit-log.controller';
import { AuditInterceptor } from '../common/interceptors/audit.interceptor';
import { ResponseService } from '../common/services/response.service';

@Global()
@Module({
  controllers: [AuditLogController],
  providers: [AuditLogService, AuditInterceptor, ResponseService],
  exports: [AuditLogService, AuditInterceptor],
})
export class AuditLogModule {}
```

(`ResponseService` — лёгкий класс без зависимостей, в проекте уже провайдится по одному экземпляру на модуль в каждом из ~13 модулей, см. `src/posts/posts.module.ts` — следуем тому же паттерну.)

- [ ] **Step 3: Проверить сборку**

Run: `npx tsc --noEmit -p tsconfig.build.json`
Expected: без ошибок

- [ ] **Step 4: Ручная проверка через dev-сервер**

Run: `npm run start:dev` (в отдельном терминале, дождаться `Nest application successfully started`), затем:

```bash
curl -i http://localhost:3333/audit-log
```

Expected: `401 Unauthorized` (эндпоинт защищён `JwtAuthGuard`, записей пока и не может быть — таблица пустая, но сначала нужно проверить, что guard действительно применяется). Остановить dev-сервер (Ctrl+C) после проверки.

- [ ] **Step 5: Commit**

```bash
git add src/audit-log/audit-log.controller.ts src/audit-log/audit-log.module.ts
git commit -m "feat: добавить GET /audit-log"
```

---

### Task 7: Применить `@Audited` к `posts` (референсная реализация)

**Files:**
- Modify: `src/posts/posts.controller.ts`
- Test: `src/posts/posts.controller.audit.spec.ts`

**Interfaces:**
- Consumes: `Audited` (Task 2), `AuditInterceptor` (Task 4).

- [ ] **Step 1: Написать падающий тест**

Создать `src/posts/posts.controller.audit.spec.ts`:

```ts
// PostsController тянет PostsService -> PrismaService и Prisma напрямую из
// generated/prisma — bare-путь не резолвится в jest. Подменяем заглушками.
jest.mock('../prisma.service', () => ({ PrismaService: class {} }));
jest.mock('generated/prisma', () => ({}));

import { Reflector } from '@nestjs/core';
import { PostsController } from './posts.controller';
import { AUDITED_ENTITY_KEY } from '../common/decorators/audited.decorator';

describe('PostsController audit metadata', () => {
  const reflector = new Reflector();

  it('помечает create() как Audited("Post")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      PostsController.prototype.create,
    );
    expect(metadata).toBe('Post');
  });

  it('помечает update() как Audited("Post")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      PostsController.prototype.update,
    );
    expect(metadata).toBe('Post');
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx jest posts.controller.audit.spec.ts`
Expected: FAIL — `metadata` равен `undefined`, `expect(undefined).toBe('Post')` не проходит.

- [ ] **Step 3: Навесить декоратор на контроллер**

В `src/posts/posts.controller.ts` заменить блок импортов (строки 1-15):

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Query,
  UseGuards,
} from '@nestjs/common';
import { PostsService } from './posts.service';
import { CreatePostDto } from './dto/create-post.dto';
import { UpdatePostDto } from './dto/update-post.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
```

на:

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Query,
  UseGuards,
  UseInterceptors,
} from '@nestjs/common';
import { PostsService } from './posts.service';
import { CreatePostDto } from './dto/create-post.dto';
import { UpdatePostDto } from './dto/update-post.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { Audited } from '../common/decorators/audited.decorator';
import { AuditInterceptor } from '../common/interceptors/audit.interceptor';
```

Затем метод `create` (строки 33-37):

```ts
  @UseGuards(JwtAuthGuard)
  @Post()
  create(@Body() createPostDto: CreatePostDto) {
    return this.postsService.create(createPostDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Post')
  @Post()
  create(@Body() createPostDto: CreatePostDto) {
    return this.postsService.create(createPostDto);
  }
```

И метод `update` (строки 57-61):

```ts
  @UseGuards(JwtAuthGuard)
  @Patch(':id')
  update(@Param('id') id: string, @Body() updatePostDto: UpdatePostDto) {
    return this.postsService.update(id, updatePostDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Post')
  @Patch(':id')
  update(@Param('id') id: string, @Body() updatePostDto: UpdatePostDto) {
    return this.postsService.update(id, updatePostDto);
  }
```

- [ ] **Step 4: Прогнать тест, убедиться, что проходит**

Run: `npx jest posts.controller.audit.spec.ts`
Expected: PASS (2 теста)

- [ ] **Step 5: Ручная сквозная проверка**

Run: `npm run start:dev` (отдельный терминал), затем — получить токен и создать пост:

```bash
TOKEN=$(curl -s -X POST http://localhost:3333/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"<ваш логин>","password":"<ваш пароль>"}' | jq -r .access_token)

curl -s -X PATCH http://localhost:3333/posts/<id существующего поста> \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"title":"audit-log smoke test"}'

curl -s "http://localhost:3333/audit-log?entityType=Post" \
  -H "Authorization: Bearer $TOKEN" | jq .
```

Expected: в ответе `GET /audit-log` есть свежая запись с `entityType: "Post"`, `action: "UPDATE"`, `entityId`, совпадающим с id поста, и `user`, совпадающим с вашим пользователем. Остановить dev-сервер после проверки.

- [ ] **Step 6: Commit**

```bash
git add src/posts/posts.controller.ts src/posts/posts.controller.audit.spec.ts
git commit -m "feat: логировать изменения постов в audit log"
```

---

### Task 8: Применить `@Audited` к `departments`

**Files:**
- Modify: `src/departments/departments.controller.ts`
- Test: `src/departments/departments.controller.audit.spec.ts`

- [ ] **Step 1: Написать падающий тест**

Создать `src/departments/departments.controller.audit.spec.ts`:

```ts
jest.mock('../prisma.service', () => ({ PrismaService: class {} }));

import { Reflector } from '@nestjs/core';
import { DepartmentsController } from './departments.controller';
import { AUDITED_ENTITY_KEY } from '../common/decorators/audited.decorator';

describe('DepartmentsController audit metadata', () => {
  const reflector = new Reflector();

  it('помечает create() как Audited("Department")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      DepartmentsController.prototype.create,
    );
    expect(metadata).toBe('Department');
  });

  it('помечает update() как Audited("Department")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      DepartmentsController.prototype.update,
    );
    expect(metadata).toBe('Department');
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx jest departments.controller.audit.spec.ts`
Expected: FAIL (metadata `undefined`)

- [ ] **Step 3: Навесить декоратор**

В `src/departments/departments.controller.ts` блок импортов (строки 1-15):

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Query,
  UseGuards,
} from '@nestjs/common';
import { DepartmentsService } from './departments.service';
import { CreateDepartmentDto } from './dto/create-department.dto';
import { UpdateDepartmentDto } from './dto/update-department.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
```

заменить на:

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Query,
  UseGuards,
  UseInterceptors,
} from '@nestjs/common';
import { DepartmentsService } from './departments.service';
import { CreateDepartmentDto } from './dto/create-department.dto';
import { UpdateDepartmentDto } from './dto/update-department.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { Audited } from '../common/decorators/audited.decorator';
import { AuditInterceptor } from '../common/interceptors/audit.interceptor';
```

Метод `create` (строки 27-31):

```ts
  @UseGuards(JwtAuthGuard)
  @Post()
  create(@Body() createDepartmentDto: CreateDepartmentDto) {
    return this.departmentsService.create(createDepartmentDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Department')
  @Post()
  create(@Body() createDepartmentDto: CreateDepartmentDto) {
    return this.departmentsService.create(createDepartmentDto);
  }
```

Метод `update` (строки 43-50):

```ts
  @UseGuards(JwtAuthGuard)
  @Patch(':id')
  update(
    @Param('id') id: string,
    @Body() updateDepartmentDto: UpdateDepartmentDto,
  ) {
    return this.departmentsService.update(id, updateDepartmentDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Department')
  @Patch(':id')
  update(
    @Param('id') id: string,
    @Body() updateDepartmentDto: UpdateDepartmentDto,
  ) {
    return this.departmentsService.update(id, updateDepartmentDto);
  }
```

- [ ] **Step 4: Прогнать тест**

Run: `npx jest departments.controller.audit.spec.ts`
Expected: PASS (2 теста)

- [ ] **Step 5: Commit**

```bash
git add src/departments/departments.controller.ts src/departments/departments.controller.audit.spec.ts
git commit -m "feat: логировать изменения отделов в audit log"
```

---

### Task 9: Применить `@Audited` к `tags`

**Files:**
- Modify: `src/tags/tags.controller.ts`
- Test: `src/tags/tags.controller.audit.spec.ts`

- [ ] **Step 1: Написать падающий тест**

Создать `src/tags/tags.controller.audit.spec.ts` (по образцу Task 8, с `TagsController` и `'Tag'`):

```ts
jest.mock('../prisma.service', () => ({ PrismaService: class {} }));

import { Reflector } from '@nestjs/core';
import { TagsController } from './tags.controller';
import { AUDITED_ENTITY_KEY } from '../common/decorators/audited.decorator';

describe('TagsController audit metadata', () => {
  const reflector = new Reflector();

  it('помечает create() как Audited("Tag")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      TagsController.prototype.create,
    );
    expect(metadata).toBe('Tag');
  });

  it('помечает update() как Audited("Tag")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      TagsController.prototype.update,
    );
    expect(metadata).toBe('Tag');
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx jest tags.controller.audit.spec.ts`
Expected: FAIL

- [ ] **Step 3: Навесить декоратор**

В `src/tags/tags.controller.ts` блок импортов (строки 1-16):

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Delete,
  Query,
  UseGuards,
} from '@nestjs/common';
import { TagsService } from './tags.service';
import { CreateTagDto } from './dto/create-tag.dto';
import { UpdateTagDto } from './dto/update-tag.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
```

заменить на:

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Delete,
  Query,
  UseGuards,
  UseInterceptors,
} from '@nestjs/common';
import { TagsService } from './tags.service';
import { CreateTagDto } from './dto/create-tag.dto';
import { UpdateTagDto } from './dto/update-tag.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { Audited } from '../common/decorators/audited.decorator';
import { AuditInterceptor } from '../common/interceptors/audit.interceptor';
```

Метод `create` (строки 28-32):

```ts
  @UseGuards(JwtAuthGuard)
  @Post()
  create(@Body() createTagDto: CreateTagDto) {
    return this.tagsService.create(createTagDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Tag')
  @Post()
  create(@Body() createTagDto: CreateTagDto) {
    return this.tagsService.create(createTagDto);
  }
```

Метод `update` (строки 44-48):

```ts
  @UseGuards(JwtAuthGuard)
  @Patch(':id')
  update(@Param('id') id: string, @Body() updateTagDto: UpdateTagDto) {
    return this.tagsService.update(+id, updateTagDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Tag')
  @Patch(':id')
  update(@Param('id') id: string, @Body() updateTagDto: UpdateTagDto) {
    return this.tagsService.update(+id, updateTagDto);
  }
```

- [ ] **Step 4: Прогнать тест**

Run: `npx jest tags.controller.audit.spec.ts`
Expected: PASS (2 теста)

- [ ] **Step 5: Commit**

```bash
git add src/tags/tags.controller.ts src/tags/tags.controller.audit.spec.ts
git commit -m "feat: логировать изменения тегов в audit log"
```

---

### Task 10: Применить `@Audited` к `achievements` (+ добавить `JwtAuthGuard`)

**Files:**
- Modify: `src/achievements/achievements.controller.ts`
- Test: `src/achievements/achievements.controller.audit.spec.ts`

**Важно:** сейчас у этого контроллера `create`/`update` вообще не защищены `JwtAuthGuard` (единственный такой случай, кроме `clubs`) — без гарда `request.user` пуст, и в лог некого записать. Добавляем `JwtAuthGuard` вместе с аудитом.

- [ ] **Step 1: Написать падающий тест**

Создать `src/achievements/achievements.controller.audit.spec.ts`:

```ts
jest.mock('../prisma.service', () => ({ PrismaService: class {} }));

import { Reflector } from '@nestjs/core';
import { AchievementsController } from './achievements.controller';
import { AUDITED_ENTITY_KEY } from '../common/decorators/audited.decorator';

describe('AchievementsController audit metadata', () => {
  const reflector = new Reflector();

  it('помечает create() как Audited("Achievement")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      AchievementsController.prototype.create,
    );
    expect(metadata).toBe('Achievement');
  });

  it('помечает update() как Audited("Achievement")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      AchievementsController.prototype.update,
    );
    expect(metadata).toBe('Achievement');
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx jest achievements.controller.audit.spec.ts`
Expected: FAIL

- [ ] **Step 3: Добавить `JwtAuthGuard` и `@Audited`**

Заменить весь файл `src/achievements/achievements.controller.ts`:

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  UseGuards,
  UseInterceptors,
} from '@nestjs/common';
import { AchievementsService } from './achievements.service';
import { CreateAchievementDto } from './dto/create-achievement.dto';
import { UpdateAchievementDto } from './dto/update-achievement.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { Audited } from '../common/decorators/audited.decorator';
import { AuditInterceptor } from '../common/interceptors/audit.interceptor';

@Controller('achievements')
export class AchievementsController {
  constructor(private readonly achievementsService: AchievementsService) {}

  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Achievement')
  @Post()
  create(@Body() createAchievementDto: CreateAchievementDto) {
    return this.achievementsService.create(createAchievementDto);
  }

  @Get()
  findAll() {
    return this.achievementsService.findAll();
  }

  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Achievement')
  @Patch(':id')
  update(
    @Param('id') id: string,
    @Body() updateAchievementDto: UpdateAchievementDto,
  ) {
    return this.achievementsService.update(id, updateAchievementDto);
  }
}
```

- [ ] **Step 4: Прогнать тест**

Run: `npx jest achievements.controller.audit.spec.ts`
Expected: PASS (2 теста)

- [ ] **Step 5: Commit**

```bash
git add src/achievements/achievements.controller.ts src/achievements/achievements.controller.audit.spec.ts
git commit -m "feat: логировать изменения достижений в audit log, закрыть create/update JwtAuthGuard"
```

---

### Task 11: Применить `@Audited` к `book`

**Files:**
- Modify: `src/book/book.controller.ts`
- Test: `src/book/book.controller.audit.spec.ts`

- [ ] **Step 1: Написать падающий тест**

Создать `src/book/book.controller.audit.spec.ts`:

```ts
jest.mock('../prisma.service', () => ({ PrismaService: class {} }));

import { Reflector } from '@nestjs/core';
import { BookController } from './book.controller';
import { AUDITED_ENTITY_KEY } from '../common/decorators/audited.decorator';

describe('BookController audit metadata', () => {
  const reflector = new Reflector();

  it('помечает create() как Audited("Book")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      BookController.prototype.create,
    );
    expect(metadata).toBe('Book');
  });

  it('помечает update() как Audited("Book")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      BookController.prototype.update,
    );
    expect(metadata).toBe('Book');
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx jest book.controller.audit.spec.ts`
Expected: FAIL

- [ ] **Step 3: Навесить декоратор**

В `src/book/book.controller.ts` блок импортов (строки 1-15):

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Query,
  UseGuards,
} from '@nestjs/common';
import { BookService } from './book.service';
import { CreateBookDto } from './dto/create-book.dto';
import { UpdateBookDto } from './dto/update-book.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
```

заменить на:

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Query,
  UseGuards,
  UseInterceptors,
} from '@nestjs/common';
import { BookService } from './book.service';
import { CreateBookDto } from './dto/create-book.dto';
import { UpdateBookDto } from './dto/update-book.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { Audited } from '../common/decorators/audited.decorator';
import { AuditInterceptor } from '../common/interceptors/audit.interceptor';
```

Метод `create` (строки 21-25):

```ts
  @UseGuards(JwtAuthGuard)
  @Post()
  create(@Body() createBookDto: CreateBookDto) {
    return this.bookService.create(createBookDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Book')
  @Post()
  create(@Body() createBookDto: CreateBookDto) {
    return this.bookService.create(createBookDto);
  }
```

Метод `update` (строки 46-50):

```ts
  @UseGuards(JwtAuthGuard)
  @Patch(':id')
  update(@Param('id') id: string, @Body() updateBookDto: UpdateBookDto) {
    return this.bookService.update(id, updateBookDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Book')
  @Patch(':id')
  update(@Param('id') id: string, @Body() updateBookDto: UpdateBookDto) {
    return this.bookService.update(id, updateBookDto);
  }
```

- [ ] **Step 4: Прогнать тест**

Run: `npx jest book.controller.audit.spec.ts`
Expected: PASS (2 теста)

- [ ] **Step 5: Commit**

```bash
git add src/book/book.controller.ts src/book/book.controller.audit.spec.ts
git commit -m "feat: логировать изменения книг в audit log"
```

---

### Task 12: Применить `@Audited` к `book-category` (модель `BookCollection`)

**Files:**
- Modify: `src/book-category/book-category.controller.ts`
- Test: `src/book-category/book-category.controller.audit.spec.ts`

**Важно:** модуль называется `book-category`, но Prisma-модель — `BookCollection` (см. `src/book-category/book-category.service.ts:18` — `this.prismaService.bookCollection.create`). `entityType` для `@Audited` — `'BookCollection'`, а не `'BookCategory'`.

- [ ] **Step 1: Написать падающий тест**

Создать `src/book-category/book-category.controller.audit.spec.ts`:

```ts
jest.mock('../prisma.service', () => ({ PrismaService: class {} }));

import { Reflector } from '@nestjs/core';
import { BookCategoryController } from './book-category.controller';
import { AUDITED_ENTITY_KEY } from '../common/decorators/audited.decorator';

describe('BookCategoryController audit metadata', () => {
  const reflector = new Reflector();

  it('помечает create() как Audited("BookCollection")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      BookCategoryController.prototype.create,
    );
    expect(metadata).toBe('BookCollection');
  });

  it('помечает update() как Audited("BookCollection")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      BookCategoryController.prototype.update,
    );
    expect(metadata).toBe('BookCollection');
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx jest book-category.controller.audit.spec.ts`
Expected: FAIL

- [ ] **Step 3: Навесить декоратор**

В `src/book-category/book-category.controller.ts` блок импортов (строки 1-15):

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  UseGuards,
  Query,
} from '@nestjs/common';
import { BookCategoryService } from './book-category.service';
import { CreateBookCategoryDto } from './dto/create-book-category.dto';
import { UpdateBookCategoryDto } from './dto/update-book-category.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
```

заменить на:

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  UseGuards,
  UseInterceptors,
  Query,
} from '@nestjs/common';
import { BookCategoryService } from './book-category.service';
import { CreateBookCategoryDto } from './dto/create-book-category.dto';
import { UpdateBookCategoryDto } from './dto/update-book-category.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { Audited } from '../common/decorators/audited.decorator';
import { AuditInterceptor } from '../common/interceptors/audit.interceptor';
```

Метод `create` (строки 21-25):

```ts
  @UseGuards(JwtAuthGuard)
  @Post()
  create(@Body() createBookCategoryDto: CreateBookCategoryDto) {
    return this.bookCategoryService.create(createBookCategoryDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('BookCollection')
  @Post()
  create(@Body() createBookCategoryDto: CreateBookCategoryDto) {
    return this.bookCategoryService.create(createBookCategoryDto);
  }
```

Метод `update` (строки 37-44):

```ts
  @UseGuards(JwtAuthGuard)
  @Patch(':id')
  update(
    @Param('id') id: string,
    @Body() updateBookCategoryDto: UpdateBookCategoryDto,
  ) {
    return this.bookCategoryService.update(id, updateBookCategoryDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('BookCollection')
  @Patch(':id')
  update(
    @Param('id') id: string,
    @Body() updateBookCategoryDto: UpdateBookCategoryDto,
  ) {
    return this.bookCategoryService.update(id, updateBookCategoryDto);
  }
```

- [ ] **Step 4: Прогнать тест**

Run: `npx jest book-category.controller.audit.spec.ts`
Expected: PASS (2 теста)

- [ ] **Step 5: Commit**

```bash
git add src/book-category/book-category.controller.ts src/book-category/book-category.controller.audit.spec.ts
git commit -m "feat: логировать изменения книжных коллекций в audit log"
```

---

### Task 13: Применить `@Audited` к `clubs` (+ добавить `JwtAuthGuard`)

**Files:**
- Modify: `src/clubs/clubs.controller.ts`
- Test: `src/clubs/clubs.controller.audit.spec.ts`

**Важно:** как и `achievements`, `create`/`update` здесь сейчас без `JwtAuthGuard`. Добавляем гард вместе с аудитом. Эндпоинт `remove` (жёсткое удаление) не трогаем — удаление вне охвата задачи.

- [ ] **Step 1: Написать падающий тест**

Создать `src/clubs/clubs.controller.audit.spec.ts`:

```ts
jest.mock('../prisma.service', () => ({ PrismaService: class {} }));

import { Reflector } from '@nestjs/core';
import { ClubsController } from './clubs.controller';
import { AUDITED_ENTITY_KEY } from '../common/decorators/audited.decorator';

describe('ClubsController audit metadata', () => {
  const reflector = new Reflector();

  it('помечает create() как Audited("Club")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      ClubsController.prototype.create,
    );
    expect(metadata).toBe('Club');
  });

  it('помечает update() как Audited("Club")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      ClubsController.prototype.update,
    );
    expect(metadata).toBe('Club');
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx jest clubs.controller.audit.spec.ts`
Expected: FAIL

- [ ] **Step 3: Добавить `JwtAuthGuard` и `@Audited`**

Заменить весь файл `src/clubs/clubs.controller.ts`:

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Delete,
  Query,
  UseGuards,
  UseInterceptors,
} from '@nestjs/common';
import { ClubsService } from './clubs.service';
import { CreateClubDto } from './dto/create-club.dto';
import { UpdateClubDto } from './dto/update-club.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { Audited } from '../common/decorators/audited.decorator';
import { AuditInterceptor } from '../common/interceptors/audit.interceptor';

@Controller('clubs')
export class ClubsController {
  constructor(private readonly clubsService: ClubsService) {}

  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Club')
  @Post()
  create(@Body() createClubDto: CreateClubDto) {
    return this.clubsService.create(createClubDto);
  }

  @Get()
  findAll(@Query() paginationQuery: PaginationQueryDto) {
    return this.clubsService.findAll(paginationQuery);
  }

  @Get(':id')
  findOne(@Param('id') id: string) {
    return this.clubsService.findOne(id);
  }

  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Club')
  @Patch(':id')
  update(@Param('id') id: string, @Body() updateClubDto: UpdateClubDto) {
    return this.clubsService.update(id, updateClubDto);
  }

  @Delete(':id')
  remove(@Param('id') id: string) {
    return this.clubsService.remove(+id);
  }
}
```

- [ ] **Step 4: Прогнать тест**

Run: `npx jest clubs.controller.audit.spec.ts`
Expected: PASS (2 теста)

- [ ] **Step 5: Commit**

```bash
git add src/clubs/clubs.controller.ts src/clubs/clubs.controller.audit.spec.ts
git commit -m "feat: логировать изменения клубов в audit log, закрыть create/update JwtAuthGuard"
```

---

### Task 14: Применить `@Audited` к `event`

**Files:**
- Modify: `src/event/event.controller.ts`
- Test: `src/event/event.controller.audit.spec.ts`

- [ ] **Step 1: Написать падающий тест**

Создать `src/event/event.controller.audit.spec.ts`:

```ts
jest.mock('../prisma.service', () => ({ PrismaService: class {} }));

import { Reflector } from '@nestjs/core';
import { EventController } from './event.controller';
import { AUDITED_ENTITY_KEY } from '../common/decorators/audited.decorator';

describe('EventController audit metadata', () => {
  const reflector = new Reflector();

  it('помечает create() как Audited("Event")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      EventController.prototype.create,
    );
    expect(metadata).toBe('Event');
  });

  it('помечает update() как Audited("Event")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      EventController.prototype.update,
    );
    expect(metadata).toBe('Event');
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx jest event.controller.audit.spec.ts`
Expected: FAIL

- [ ] **Step 3: Навесить декоратор**

В `src/event/event.controller.ts` блок импортов (строки 1-15):

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Query,
  UseGuards,
} from '@nestjs/common';
import { EventService } from './event.service';
import { CreateEventDto } from './dto/create-event.dto';
import { UpdateEventDto } from './dto/update-event.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
```

заменить на:

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Query,
  UseGuards,
  UseInterceptors,
} from '@nestjs/common';
import { EventService } from './event.service';
import { CreateEventDto } from './dto/create-event.dto';
import { UpdateEventDto } from './dto/update-event.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { Audited } from '../common/decorators/audited.decorator';
import { AuditInterceptor } from '../common/interceptors/audit.interceptor';
```

Метод `create` (строки 27-31):

```ts
  @UseGuards(JwtAuthGuard)
  @Post()
  create(@Body() createEventDto: CreateEventDto) {
    return this.eventService.create(createEventDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Event')
  @Post()
  create(@Body() createEventDto: CreateEventDto) {
    return this.eventService.create(createEventDto);
  }
```

Метод `update` (строки 43-47):

```ts
  @UseGuards(JwtAuthGuard)
  @Patch(':id')
  update(@Param('id') id: string, @Body() updateEventDto: UpdateEventDto) {
    return this.eventService.update(id, updateEventDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Event')
  @Patch(':id')
  update(@Param('id') id: string, @Body() updateEventDto: UpdateEventDto) {
    return this.eventService.update(id, updateEventDto);
  }
```

- [ ] **Step 4: Прогнать тест**

Run: `npx jest event.controller.audit.spec.ts`
Expected: PASS (2 теста)

- [ ] **Step 5: Commit**

```bash
git add src/event/event.controller.ts src/event/event.controller.audit.spec.ts
git commit -m "feat: логировать изменения событий в audit log"
```

---

### Task 15: Применить `@Audited` к `main-slider` (модель `MainSliderSlide`)

**Files:**
- Modify: `src/main-slider/main-slider.controller.ts`
- Test: `src/main-slider/main-slider.controller.audit.spec.ts`

- [ ] **Step 1: Написать падающий тест**

Создать `src/main-slider/main-slider.controller.audit.spec.ts`:

```ts
jest.mock('../prisma.service', () => ({ PrismaService: class {} }));

import { Reflector } from '@nestjs/core';
import { MainSliderController } from './main-slider.controller';
import { AUDITED_ENTITY_KEY } from '../common/decorators/audited.decorator';

describe('MainSliderController audit metadata', () => {
  const reflector = new Reflector();

  it('помечает create() как Audited("MainSliderSlide")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      MainSliderController.prototype.create,
    );
    expect(metadata).toBe('MainSliderSlide');
  });

  it('помечает update() как Audited("MainSliderSlide")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      MainSliderController.prototype.update,
    );
    expect(metadata).toBe('MainSliderSlide');
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx jest main-slider.controller.audit.spec.ts`
Expected: FAIL

- [ ] **Step 3: Навесить декоратор**

В `src/main-slider/main-slider.controller.ts` блок импортов (строки 1-15):

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Query,
  UseGuards,
} from '@nestjs/common';
import { MainSliderService } from './main-slider.service';
import { CreateMainSliderDto } from './dto/create-main-slider.dto';
import { UpdateMainSliderDto } from './dto/update-main-slider.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
```

заменить на:

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Query,
  UseGuards,
  UseInterceptors,
} from '@nestjs/common';
import { MainSliderService } from './main-slider.service';
import { CreateMainSliderDto } from './dto/create-main-slider.dto';
import { UpdateMainSliderDto } from './dto/update-main-slider.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { Audited } from '../common/decorators/audited.decorator';
import { AuditInterceptor } from '../common/interceptors/audit.interceptor';
```

Метод `create` (строки 27-31):

```ts
  @UseGuards(JwtAuthGuard)
  @Post()
  create(@Body() createMainSliderDto: CreateMainSliderDto) {
    return this.mainSliderService.create(createMainSliderDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('MainSliderSlide')
  @Post()
  create(@Body() createMainSliderDto: CreateMainSliderDto) {
    return this.mainSliderService.create(createMainSliderDto);
  }
```

Метод `update` (строки 43-50):

```ts
  @UseGuards(JwtAuthGuard)
  @Patch(':id')
  update(
    @Param('id') id: string,
    @Body() updateMainSliderDto: UpdateMainSliderDto,
  ) {
    return this.mainSliderService.update(id, updateMainSliderDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('MainSliderSlide')
  @Patch(':id')
  update(
    @Param('id') id: string,
    @Body() updateMainSliderDto: UpdateMainSliderDto,
  ) {
    return this.mainSliderService.update(id, updateMainSliderDto);
  }
```

- [ ] **Step 4: Прогнать тест**

Run: `npx jest main-slider.controller.audit.spec.ts`
Expected: PASS (2 теста)

- [ ] **Step 5: Commit**

```bash
git add src/main-slider/main-slider.controller.ts src/main-slider/main-slider.controller.audit.spec.ts
git commit -m "feat: логировать изменения слайдов слайдера в audit log"
```

---

### Task 16: Применить `@Audited` к `map-point`

**Files:**
- Modify: `src/map-point/map-point.controller.ts`
- Test: `src/map-point/map-point.controller.audit.spec.ts`

- [ ] **Step 1: Написать падающий тест**

Создать `src/map-point/map-point.controller.audit.spec.ts`:

```ts
jest.mock('../prisma.service', () => ({ PrismaService: class {} }));

import { Reflector } from '@nestjs/core';
import { MapPointController } from './map-point.controller';
import { AUDITED_ENTITY_KEY } from '../common/decorators/audited.decorator';

describe('MapPointController audit metadata', () => {
  const reflector = new Reflector();

  it('помечает create() как Audited("MapPoint")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      MapPointController.prototype.create,
    );
    expect(metadata).toBe('MapPoint');
  });

  it('помечает update() как Audited("MapPoint")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      MapPointController.prototype.update,
    );
    expect(metadata).toBe('MapPoint');
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx jest map-point.controller.audit.spec.ts`
Expected: FAIL

- [ ] **Step 3: Навесить декоратор**

В `src/map-point/map-point.controller.ts` блок импортов (строки 1-15):

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Query,
  UseGuards,
} from '@nestjs/common';
import { MapPointService } from './map-point.service';
import { CreateMapPointDto } from './dto/create-map-point.dto';
import { UpdateMapPointDto } from './dto/update-map-point.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
```

заменить на:

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Query,
  UseGuards,
  UseInterceptors,
} from '@nestjs/common';
import { MapPointService } from './map-point.service';
import { CreateMapPointDto } from './dto/create-map-point.dto';
import { UpdateMapPointDto } from './dto/update-map-point.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { Audited } from '../common/decorators/audited.decorator';
import { AuditInterceptor } from '../common/interceptors/audit.interceptor';
```

Метод `create` (строки 21-25):

```ts
  @UseGuards(JwtAuthGuard)
  @Post()
  create(@Body() createMapPointDto: CreateMapPointDto) {
    return this.mapPointService.create(createMapPointDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('MapPoint')
  @Post()
  create(@Body() createMapPointDto: CreateMapPointDto) {
    return this.mapPointService.create(createMapPointDto);
  }
```

Метод `update` (строки 37-44):

```ts
  @UseGuards(JwtAuthGuard)
  @Patch(':id')
  update(
    @Param('id') id: string,
    @Body() updateMapPointDto: UpdateMapPointDto,
  ) {
    return this.mapPointService.update(id, updateMapPointDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('MapPoint')
  @Patch(':id')
  update(
    @Param('id') id: string,
    @Body() updateMapPointDto: UpdateMapPointDto,
  ) {
    return this.mapPointService.update(id, updateMapPointDto);
  }
```

- [ ] **Step 4: Прогнать тест**

Run: `npx jest map-point.controller.audit.spec.ts`
Expected: PASS (2 теста)

- [ ] **Step 5: Commit**

```bash
git add src/map-point/map-point.controller.ts src/map-point/map-point.controller.audit.spec.ts
git commit -m "feat: логировать изменения точек карты в audit log"
```

---

### Task 17: Применить `@Audited` к `notification`

**Files:**
- Modify: `src/notification/notification.controller.ts`
- Test: `src/notification/notification.controller.audit.spec.ts`

- [ ] **Step 1: Написать падающий тест**

Создать `src/notification/notification.controller.audit.spec.ts`:

```ts
jest.mock('../prisma.service', () => ({ PrismaService: class {} }));

import { Reflector } from '@nestjs/core';
import { NotificationController } from './notification.controller';
import { AUDITED_ENTITY_KEY } from '../common/decorators/audited.decorator';

describe('NotificationController audit metadata', () => {
  const reflector = new Reflector();

  it('помечает create() как Audited("Notification")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      NotificationController.prototype.create,
    );
    expect(metadata).toBe('Notification');
  });

  it('помечает update() как Audited("Notification")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      NotificationController.prototype.update,
    );
    expect(metadata).toBe('Notification');
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx jest notification.controller.audit.spec.ts`
Expected: FAIL

- [ ] **Step 3: Навесить декоратор**

В `src/notification/notification.controller.ts` блок импортов (строки 1-16):

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Delete,
  Query,
  UseGuards,
} from '@nestjs/common';
import { NotificationService } from './notification.service';
import { CreateNotificationDto } from './dto/create-notification.dto';
import { UpdateNotificationDto } from './dto/update-notification.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
```

заменить на:

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Delete,
  Query,
  UseGuards,
  UseInterceptors,
} from '@nestjs/common';
import { NotificationService } from './notification.service';
import { CreateNotificationDto } from './dto/create-notification.dto';
import { UpdateNotificationDto } from './dto/update-notification.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { Audited } from '../common/decorators/audited.decorator';
import { AuditInterceptor } from '../common/interceptors/audit.interceptor';
```

Метод `create` (строки 22-26):

```ts
  @UseGuards(JwtAuthGuard)
  @Post()
  create(@Body() createNotificationDto: CreateNotificationDto) {
    return this.notificationService.create(createNotificationDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Notification')
  @Post()
  create(@Body() createNotificationDto: CreateNotificationDto) {
    return this.notificationService.create(createNotificationDto);
  }
```

Метод `update` (строки 33-40):

```ts
  @UseGuards(JwtAuthGuard)
  @Patch(':id')
  update(
    @Param('id') id: string,
    @Body() updateNotificationDto: UpdateNotificationDto,
  ) {
    return this.notificationService.update(id, updateNotificationDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Notification')
  @Patch(':id')
  update(
    @Param('id') id: string,
    @Body() updateNotificationDto: UpdateNotificationDto,
  ) {
    return this.notificationService.update(id, updateNotificationDto);
  }
```

(эндпоинт `remove` — жёсткое удаление, не трогаем)

- [ ] **Step 4: Прогнать тест**

Run: `npx jest notification.controller.audit.spec.ts`
Expected: PASS (2 теста)

- [ ] **Step 5: Commit**

```bash
git add src/notification/notification.controller.ts src/notification/notification.controller.audit.spec.ts
git commit -m "feat: логировать изменения уведомлений в audit log"
```

---

### Task 18: Применить `@Audited` к `page`

**Files:**
- Modify: `src/page/page.controller.ts`
- Test: `src/page/page.controller.audit.spec.ts`

- [ ] **Step 1: Написать падающий тест**

Создать `src/page/page.controller.audit.spec.ts`:

```ts
jest.mock('../prisma.service', () => ({ PrismaService: class {} }));
jest.mock('generated/prisma', () => ({}));

import { Reflector } from '@nestjs/core';
import { PageController } from './page.controller';
import { AUDITED_ENTITY_KEY } from '../common/decorators/audited.decorator';

describe('PageController audit metadata', () => {
  const reflector = new Reflector();

  it('помечает create() как Audited("Page")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      PageController.prototype.create,
    );
    expect(metadata).toBe('Page');
  });

  it('помечает update() как Audited("Page")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      PageController.prototype.update,
    );
    expect(metadata).toBe('Page');
  });
});
```

(`page.service.ts` импортирует типы напрямую из `generated/prisma` — мокаем оба модуля, как в Task 7 для постов.)

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx jest page.controller.audit.spec.ts`
Expected: FAIL

- [ ] **Step 3: Навесить декоратор**

В `src/page/page.controller.ts` блок импортов (строки 1-16):

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Delete,
  Query,
  UseGuards,
} from '@nestjs/common';
import { PageService } from './page.service';
import { CreatePageDto } from './dto/create-page.dto';
import { UpdatePageDto } from './dto/update-page.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
```

заменить на:

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Delete,
  Query,
  UseGuards,
  UseInterceptors,
} from '@nestjs/common';
import { PageService } from './page.service';
import { CreatePageDto } from './dto/create-page.dto';
import { UpdatePageDto } from './dto/update-page.dto';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { Audited } from '../common/decorators/audited.decorator';
import { AuditInterceptor } from '../common/interceptors/audit.interceptor';
```

Метод `create` (строки 22-26):

```ts
  @UseGuards(JwtAuthGuard)
  @Post()
  create(@Body() createPageDto: CreatePageDto) {
    return this.pageService.create(createPageDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Page')
  @Post()
  create(@Body() createPageDto: CreatePageDto) {
    return this.pageService.create(createPageDto);
  }
```

Метод `update` (строки 38-41):

```ts
  @UseGuards(JwtAuthGuard)
  @Patch(':id')
  update(@Param('id') id: string, @Body() updatePageDto: UpdatePageDto) {
    return this.pageService.update(id, updatePageDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Page')
  @Patch(':id')
  update(@Param('id') id: string, @Body() updatePageDto: UpdatePageDto) {
    return this.pageService.update(id, updatePageDto);
  }
```

(эндпоинт `remove` — жёсткое удаление, не трогаем)

- [ ] **Step 4: Прогнать тест**

Run: `npx jest page.controller.audit.spec.ts`
Expected: PASS (2 теста)

- [ ] **Step 5: Commit**

```bash
git add src/page/page.controller.ts src/page/page.controller.audit.spec.ts
git commit -m "feat: логировать изменения страниц в audit log"
```

---

### Task 19: Применить `@Audited` к `comics` (Comic, ComicGenre, ComicSeries)

**Files:**
- Modify: `src/comics/comics.controller.ts`
- Test: `src/comics/comics.controller.audit.spec.ts`

- [ ] **Step 1: Написать падающий тест**

Создать `src/comics/comics.controller.audit.spec.ts`:

```ts
jest.mock('../prisma.service', () => ({ PrismaService: class {} }));
jest.mock('generated/prisma', () => ({}));

import { Reflector } from '@nestjs/core';
import { ComicsController } from './comics.controller';
import { AUDITED_ENTITY_KEY } from '../common/decorators/audited.decorator';

describe('ComicsController audit metadata', () => {
  const reflector = new Reflector();

  it('помечает createGenre() как Audited("ComicGenre")', () => {
    expect(
      reflector.get(AUDITED_ENTITY_KEY, ComicsController.prototype.createGenre),
    ).toBe('ComicGenre');
  });

  it('помечает updateGenre() как Audited("ComicGenre")', () => {
    expect(
      reflector.get(AUDITED_ENTITY_KEY, ComicsController.prototype.updateGenre),
    ).toBe('ComicGenre');
  });

  it('помечает createSeries() как Audited("ComicSeries")', () => {
    expect(
      reflector.get(AUDITED_ENTITY_KEY, ComicsController.prototype.createSeries),
    ).toBe('ComicSeries');
  });

  it('помечает updateSeries() как Audited("ComicSeries")', () => {
    expect(
      reflector.get(AUDITED_ENTITY_KEY, ComicsController.prototype.updateSeries),
    ).toBe('ComicSeries');
  });

  it('помечает create() как Audited("Comic")', () => {
    expect(
      reflector.get(AUDITED_ENTITY_KEY, ComicsController.prototype.create),
    ).toBe('Comic');
  });

  it('помечает update() как Audited("Comic")', () => {
    expect(
      reflector.get(AUDITED_ENTITY_KEY, ComicsController.prototype.update),
    ).toBe('Comic');
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx jest comics.controller.audit.spec.ts`
Expected: FAIL

- [ ] **Step 3: Навесить декораторы**

Заменить весь файл `src/comics/comics.controller.ts`:

```ts
import {
  Body,
  Controller,
  Get,
  Param,
  Patch,
  Post,
  Query,
  UseGuards,
  UseInterceptors,
} from '@nestjs/common';
import { ComicsService } from './comics.service';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { CreateComicDto } from './dto/create-comic.dto';
import { UpdateComicDto } from './dto/update-comic.dto';
import { CreateComicGenreDto } from './dto/create-comic-genre.dto';
import { CreateComicSeriesDto } from './dto/create-comic-series.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { Audited } from '../common/decorators/audited.decorator';
import { AuditInterceptor } from '../common/interceptors/audit.interceptor';

@Controller('comics')
export class ComicsController {
  constructor(private readonly comicsService: ComicsService) {}

  @Get('genres')
  findAllGenres() {
    return this.comicsService.findAllGenres();
  }

  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('ComicGenre')
  @Post('genres')
  createGenre(@Body() dto: CreateComicGenreDto) {
    return this.comicsService.createGenre(dto);
  }

  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('ComicGenre')
  @Patch('genres/:id')
  updateGenre(
    @Param('id') id: string,
    @Body() dto: Partial<CreateComicGenreDto>,
  ) {
    return this.comicsService.updateGenre(id, dto);
  }

  @Get('series')
  findAllSeries() {
    return this.comicsService.findAllSeries();
  }

  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('ComicSeries')
  @Post('series')
  createSeries(@Body() dto: CreateComicSeriesDto) {
    return this.comicsService.createSeries(dto);
  }

  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('ComicSeries')
  @Patch('series/:id')
  updateSeries(
    @Param('id') id: string,
    @Body() dto: Partial<CreateComicSeriesDto>,
  ) {
    return this.comicsService.updateSeries(id, dto);
  }

  @Get()
  findAll(@Query() paginationQuery: PaginationQueryDto) {
    return this.comicsService.findAll(paginationQuery);
  }

  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Comic')
  @Post()
  create(@Body() dto: CreateComicDto) {
    return this.comicsService.create(dto);
  }

  @Get(':id')
  findOne(@Param('id') id: string) {
    return this.comicsService.findOne(id);
  }

  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Comic')
  @Patch(':id')
  update(@Param('id') id: string, @Body() dto: UpdateComicDto) {
    return this.comicsService.update(id, dto);
  }
}
```

- [ ] **Step 4: Прогнать тест**

Run: `npx jest comics.controller.audit.spec.ts`
Expected: PASS (6 тестов)

- [ ] **Step 5: Commit**

```bash
git add src/comics/comics.controller.ts src/comics/comics.controller.audit.spec.ts
git commit -m "feat: логировать изменения комиксов, жанров и серий в audit log"
```

---

### Task 20: Применить `@Audited` к `games` (Game, GameGenre, GameSeries)

**Files:**
- Modify: `src/games/games.controller.ts`
- Test: `src/games/games.controller.audit.spec.ts`

- [ ] **Step 1: Написать падающий тест**

Создать `src/games/games.controller.audit.spec.ts`:

```ts
jest.mock('../prisma.service', () => ({ PrismaService: class {} }));
jest.mock('generated/prisma', () => ({}));

import { Reflector } from '@nestjs/core';
import { GamesController } from './games.controller';
import { AUDITED_ENTITY_KEY } from '../common/decorators/audited.decorator';

describe('GamesController audit metadata', () => {
  const reflector = new Reflector();

  it('помечает createGenre() как Audited("GameGenre")', () => {
    expect(
      reflector.get(AUDITED_ENTITY_KEY, GamesController.prototype.createGenre),
    ).toBe('GameGenre');
  });

  it('помечает updateGenre() как Audited("GameGenre")', () => {
    expect(
      reflector.get(AUDITED_ENTITY_KEY, GamesController.prototype.updateGenre),
    ).toBe('GameGenre');
  });

  it('помечает createSeries() как Audited("GameSeries")', () => {
    expect(
      reflector.get(AUDITED_ENTITY_KEY, GamesController.prototype.createSeries),
    ).toBe('GameSeries');
  });

  it('помечает updateSeries() как Audited("GameSeries")', () => {
    expect(
      reflector.get(AUDITED_ENTITY_KEY, GamesController.prototype.updateSeries),
    ).toBe('GameSeries');
  });

  it('помечает create() как Audited("Game")', () => {
    expect(
      reflector.get(AUDITED_ENTITY_KEY, GamesController.prototype.create),
    ).toBe('Game');
  });

  it('помечает update() как Audited("Game")', () => {
    expect(
      reflector.get(AUDITED_ENTITY_KEY, GamesController.prototype.update),
    ).toBe('Game');
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx jest games.controller.audit.spec.ts`
Expected: FAIL

- [ ] **Step 3: Навесить декораторы**

Заменить весь файл `src/games/games.controller.ts`:

```ts
import {
  Body,
  Controller,
  Get,
  Param,
  Patch,
  Post,
  Query,
  UseGuards,
  UseInterceptors,
} from '@nestjs/common';
import { GamesService } from './games.service';
import { PaginationQueryDto } from '../common/dto/pagination-query.dto';
import { CreateGameDto } from './dto/create-game.dto';
import { UpdateGameDto } from './dto/update-game.dto';
import { CreateGameGenreDto } from './dto/create-game-genre.dto';
import { CreateGameSeriesDto } from './dto/create-game-series.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { Audited } from '../common/decorators/audited.decorator';
import { AuditInterceptor } from '../common/interceptors/audit.interceptor';

@Controller('games')
export class GamesController {
  constructor(private readonly gamesService: GamesService) {}

  @UseGuards(JwtAuthGuard)
  @Get('migrate')
  migrate() {
    return this.gamesService.migrate();
  }

  @UseGuards(JwtAuthGuard)
  @Get('regroup-series')
  regroupSeries() {
    return this.gamesService.regroupSeries();
  }

  @UseGuards(JwtAuthGuard)
  @Get('backfill-created-at')
  backfillCreatedAt() {
    return this.gamesService.backfillCreatedAtFromExternalId();
  }

  @Get('genres')
  findAllGenres() {
    return this.gamesService.findAllGenres();
  }

  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('GameGenre')
  @Post('genres')
  createGenre(@Body() dto: CreateGameGenreDto) {
    return this.gamesService.createGenre(dto);
  }

  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('GameGenre')
  @Patch('genres/:id')
  updateGenre(
    @Param('id') id: string,
    @Body() dto: Partial<CreateGameGenreDto>,
  ) {
    return this.gamesService.updateGenre(id, dto);
  }

  @Get('series')
  findAllSeries() {
    return this.gamesService.findAllSeries();
  }

  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('GameSeries')
  @Post('series')
  createSeries(@Body() dto: CreateGameSeriesDto) {
    return this.gamesService.createSeries(dto);
  }

  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('GameSeries')
  @Patch('series/:id')
  updateSeries(
    @Param('id') id: string,
    @Body() dto: Partial<CreateGameSeriesDto>,
  ) {
    return this.gamesService.updateSeries(id, dto);
  }

  @Get()
  findAll(@Query() paginationQuery: PaginationQueryDto) {
    return this.gamesService.findAll(paginationQuery);
  }

  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Game')
  @Post()
  create(@Body() dto: CreateGameDto) {
    return this.gamesService.create(dto);
  }

  @Get(':id')
  findOne(@Param('id') id: string) {
    return this.gamesService.findOne(id);
  }

  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('Game')
  @Patch(':id')
  update(@Param('id') id: string, @Body() dto: UpdateGameDto) {
    return this.gamesService.update(id, dto);
  }
}
```

- [ ] **Step 4: Прогнать тест**

Run: `npx jest games.controller.audit.spec.ts`
Expected: PASS (6 тестов)

- [ ] **Step 5: Commit**

```bash
git add src/games/games.controller.ts src/games/games.controller.audit.spec.ts
git commit -m "feat: логировать изменения игр, жанров и серий в audit log"
```

---

### Task 21: Применить `@Audited` к `navigation-item` (без `batch-update`)

**Files:**
- Modify: `src/navigation-item/navigation-item.controller.ts`
- Test: `src/navigation-item/navigation-item.controller.audit.spec.ts`

**Важно:** эндпоинт `POST /navigation-item/batch-update` (`updateOrderBatch`) не помечается — он принимает массив и не возвращает одну сущность с `id`, `@Audited` на нём не сработал бы осмысленно (см. Global Constraints и спеку).

- [ ] **Step 1: Написать падающий тест**

Создать `src/navigation-item/navigation-item.controller.audit.spec.ts`:

```ts
jest.mock('../prisma.service', () => ({ PrismaService: class {} }));

import { Reflector } from '@nestjs/core';
import { NavigationItemController } from './navigation-item.controller';
import { AUDITED_ENTITY_KEY } from '../common/decorators/audited.decorator';

describe('NavigationItemController audit metadata', () => {
  const reflector = new Reflector();

  it('помечает create() как Audited("NavigationItem")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      NavigationItemController.prototype.create,
    );
    expect(metadata).toBe('NavigationItem');
  });

  it('помечает update() как Audited("NavigationItem")', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      NavigationItemController.prototype.update,
    );
    expect(metadata).toBe('NavigationItem');
  });

  it('НЕ помечает updateOrderBatch() (массовая правка порядка, не одна сущность)', () => {
    const metadata = reflector.get(
      AUDITED_ENTITY_KEY,
      NavigationItemController.prototype.updateOrderBatch,
    );
    expect(metadata).toBeUndefined();
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx jest navigation-item.controller.audit.spec.ts`
Expected: FAIL (первые два `expect` получают `undefined` вместо `'NavigationItem'`)

- [ ] **Step 3: Навесить декоратор**

В `src/navigation-item/navigation-item.controller.ts` блок импортов (строки 1-14):

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  UseGuards,
} from '@nestjs/common';
import { NavigationItemService } from './navigation-item.service';
import { CreateNavigationItemDto } from './dto/create-navigation-item.dto';
import { UpdateNavigationItemDto } from './dto/update-navigation-item.dto';
import { BatchUpdateNavigationItemDto } from './dto/update-batch.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
```

заменить на:

```ts
import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  UseGuards,
  UseInterceptors,
} from '@nestjs/common';
import { NavigationItemService } from './navigation-item.service';
import { CreateNavigationItemDto } from './dto/create-navigation-item.dto';
import { UpdateNavigationItemDto } from './dto/update-navigation-item.dto';
import { BatchUpdateNavigationItemDto } from './dto/update-batch.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { Audited } from '../common/decorators/audited.decorator';
import { AuditInterceptor } from '../common/interceptors/audit.interceptor';
```

Метод `create` (строки 20-24):

```ts
  @UseGuards(JwtAuthGuard)
  @Post()
  create(@Body() createNavigationItemDto: CreateNavigationItemDto) {
    return this.navigationItemService.create(createNavigationItemDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('NavigationItem')
  @Post()
  create(@Body() createNavigationItemDto: CreateNavigationItemDto) {
    return this.navigationItemService.create(createNavigationItemDto);
  }
```

Метод `update` (строки 52-59) — `batch-update` (строки 26-30) не трогаем:

```ts
  @UseGuards(JwtAuthGuard)
  @Patch(':id')
  update(
    @Param('id') id: string,
    @Body() updateNavigationItemDto: UpdateNavigationItemDto,
  ) {
    return this.navigationItemService.update(id, updateNavigationItemDto);
  }
```

заменить на:

```ts
  @UseGuards(JwtAuthGuard)
  @UseInterceptors(AuditInterceptor)
  @Audited('NavigationItem')
  @Patch(':id')
  update(
    @Param('id') id: string,
    @Body() updateNavigationItemDto: UpdateNavigationItemDto,
  ) {
    return this.navigationItemService.update(id, updateNavigationItemDto);
  }
```

- [ ] **Step 4: Прогнать тест**

Run: `npx jest navigation-item.controller.audit.spec.ts`
Expected: PASS (3 теста)

- [ ] **Step 5: Commit**

```bash
git add src/navigation-item/navigation-item.controller.ts src/navigation-item/navigation-item.controller.audit.spec.ts
git commit -m "feat: логировать изменения пунктов навигации в audit log"
```

---

### Task 22: Финальная проверка

**Files:** нет новых, только запуск проверок.

- [ ] **Step 1: Прогнать весь unit-тестовый набор**

Run: `npm run test`
Expected: все тесты (существующие плюс новые, добавленные в задачах 2-21) проходят, 0 failed.

- [ ] **Step 2: Прогнать линтер**

Run: `npm run lint`
Expected: без ошибок (предупреждения `no-floating-promises` в существующем коде — норма проекта, см. CLAUDE.md).

- [ ] **Step 3: Проверить сборку**

Run: `npm run build`
Expected: сборка завершается без ошибок TypeScript.

- [ ] **Step 4: Ручная сквозная проверка ещё одного модуля (не posts)**

Run: `npm run start:dev` (отдельный терминал), затем:

```bash
TOKEN=$(curl -s -X POST http://localhost:3333/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"<ваш логин>","password":"<ваш пароль>"}' | jq -r .access_token)

curl -s -X PATCH http://localhost:3333/departments/<id существующего отдела> \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"title":"audit-log smoke test 2"}'

curl -s "http://localhost:3333/audit-log?entityType=Department" \
  -H "Authorization: Bearer $TOKEN" | jq .
```

Expected: свежая запись с `entityType: "Department"`, `action: "UPDATE"`. Остановить dev-сервер после проверки.

- [ ] **Step 5: Финальный commit (если после lint/build были правки)**

Если `npm run lint`/`npm run build` ничего не поправили автоматически — коммитить нечего, шаг пропускается. Если правки были:

```bash
git add -A
git commit -m "chore: поправки после финальной проверки audit log"
```
