# Превью новостей на сервере: работает старая сборка

## Ответы серверного бэкенда (192.168.0.42:3300 → nomb_db)

- `files/migrate`: `total 16031, migrated 0, skipped 16031, failed 0`. Все файлы старой БД уже есть в `nomb_db`.
- `posts/migrate`: `total 4828, migrated 0, skipped 4828, failed 0`. **Поля `previewsFixed` нет.**

## Вывод

Поле `previewsFixed` добавлено исправлением миграции. Раз его нет, на сервере запущен образ, собранный до исправления. Такой код пропускает уже перенесённые новости целиком, поэтому заглушки остаются.

## Проверено

- Исправление есть в `origin/master` (коммит `3c7ae44`, Task 8 #19, 2026-09-25 16:06).
- GitHub Actions «CI/CD» для `3c7ae44` завершился `success`. Но workflow только дёргает webhook Dokploy, а упавшая сборка образа в Dokploy оставляет на сервере предыдущую версию.
- `origin/master` собирается локально (`nest build` проходит), `previewsFixed` в `dist/posts/posts.service.js` присутствует. Сборка проверялась во временном worktree, после проверки он удалён.

## Что сделать

1. В Dokploy (http://192.168.0.35:3000) открыть деплой коммита `3c7ae44`. Проверить, завершилась ли сборка и перезапущен ли контейнер. При необходимости нажать Redeploy.
2. После деплоя вызвать `GET /api/posts/migrate`: в ответе должно появиться `previewsFixed` > 0.
3. Повторять `files/migrate` не нужно, все файлы уже перенесены.
